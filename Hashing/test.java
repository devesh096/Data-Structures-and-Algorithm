public class test {
    public static void main(String[] args) {
        long diff = Math.abs(leftsum / (i + 1) - (l - i == 1 ? 0 : rightsum / (l - i - 1)));  
        if(l-1 ==1){
            long diff = Math.abs(leftsum /(i + 1) - 0 ); 
        }
        else{
            long diff = Math.abs(leftsum / (i + 1) - (rightsum / (l - i - 1)));  
        }

        
    }
}
